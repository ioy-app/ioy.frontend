stages:
  - build
  - run

Building:
  stage: build
  tags:
    - shell 
  script:
    - |
      #!/bin/bash
      npm i
      npm run build
      docker image prune -f
      docker build -t frontend .
  only:
    - develop
Running:
  stage: run
  tags:
    - shell 
  script:
    - |
      echo "Success buil a"
  only:
    - develop