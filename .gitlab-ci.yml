stages:
  - build
  - run

Building:
  stage: build
  tags:
    - shell 
  script:
    - |
      #!/bin/bash
      npm i
      npm run build
      docker build -t frontend .
  only:
    - develop
Running:
  stage: run
  tags:
    - shell 
  script:
    - |
      echo "Success build"
  only:
    - develop